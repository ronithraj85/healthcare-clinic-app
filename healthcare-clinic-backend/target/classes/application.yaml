server:
  port: 8181


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/healthcare_db
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
#spring:
#  datasource:
#    url: jdbc:postgresql://dpg-d5ciuouuk2gs7384hbs0-a:5432/healthcare_db_cwho
#    username: healthcare_db_cwho_user
#    password: SX0dlyP1kMN0MhTfoiquI4m8fMEU7txT
#
#  jpa:
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
#    hibernate:
#      ddl-auto: update

    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

logging:
  level:
    root: INFO
    com.healthcare.clinic: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

management:
  endpoints:
    web:
      exposure:
        include: health, info
